# Assets

GitHub-ready visual evidence used by the repository README.

| File | Purpose |
| --- | --- |
| `hardware-setup.png` | Jetson Nano, USB camera, power/network wiring, and physical deployment context. |
| `web-dashboard-multitarget.png` | Web dashboard showing annotated MJPEG stream, multi-target detection, FPS, policy state, and runtime panels. |
| `api-perf-json.png` | `/api/perf` response rendered as JSON, including CPU, shared memory, SWAP, GR3D, and temperature fields. |
| `tegrastats-runtime-log.png` | Runtime `tegrastats` log during `960x544` multi-target detection. |

For future updates, keep image names ASCII and avoid committing large raw videos. Prefer compressed screenshots, short MP4 links, or external demo links.
